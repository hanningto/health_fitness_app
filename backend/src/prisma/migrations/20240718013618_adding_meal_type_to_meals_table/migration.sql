BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[meals] ADD [meal_type] VARCHAR(50) NOT NULL CONSTRAINT [meals_meal_type_df] DEFAULT 'null';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
